# Variáveis de ambiente necessárias no Netlify (Site settings → Environment variables)
# Nunca commitar os valores reais — este arquivo é só um modelo.

DAPIC_IDENTIFICADOR=studiolorenavieira
DAPIC_TOKEN=
